services:
  postgres:
    image: postgres:16-alpine
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: tableside
      POSTGRES_PASSWORD: tableside
      POSTGRES_DB: tableside
    volumes:
      - postgres-data:/var/lib/postgresql/data

volumes:
  postgres-data:
