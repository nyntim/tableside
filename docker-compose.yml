services:
  postgres:
    image: postgres:16-alpine
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: odyssey
      POSTGRES_PASSWORD: odyssey
      POSTGRES_DB: odyssey_ops
    volumes:
      - postgres-data:/var/lib/postgresql/data

volumes:
  postgres-data:
